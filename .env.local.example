# Supabase Configuration (Auth & Database only)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key (Server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AWS S3 Configuration (Video Storage)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_S3_BUCKET_NAME=speaking-coach-videos

# Google Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# AI Model Endpoint (Optional - for custom model integration later)
# The external AI service endpoint that analyzes video content and returns feedback
# This endpoint should accept POST requests with a JSON body containing a videoUrl
# Expected request format: { "videoUrl": "https://signed-url-to-video.mp4" }
# Expected response format: { "feedback": "Your detailed feedback text here..." }
# The endpoint should return a 200 status code on success
# AI_MODEL_ENDPOINT=https://your-model-endpoint.com/analyze
